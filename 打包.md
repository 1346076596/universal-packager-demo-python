# 通用自动打包使用说明

这个仓库是一套“上传代码后自动打包”的 GitHub Actions 模板。

最简单的使用方式是：从本仓库创建一个新仓库，把程序源码上传到新仓库的根目录，然后等待 Actions 完成。成功后可以在仓库的 **Releases** 页面直接下载 Windows、Linux、macOS 或 Android 产物。

## 1. 支持范围

| 语言或框架 | 自动识别标志 | 默认产物 |
| --- | --- | --- |
| Python | `pyproject.toml`、`requirements.txt`、`main.py` 等 | PyInstaller 单文件程序 |
| Node.js / TypeScript | `package.json` | Node CLI 可执行文件，或 `dist/build/out` Web 包 |
| Electron | `electron`、`electron-builder` 依赖 | 各桌面系统安装包或应用目录 |
| Tauri | `@tauri-apps/*`、Cargo | 各桌面系统原生安装包 |
| Go | `go.mod` | 原生单文件程序 |
| Rust | `Cargo.toml` | `cargo build --release` 原生程序 |
| C# / F# / VB.NET | `*.csproj`、`*.fsproj`、`*.vbproj` | 自包含、单文件 .NET 程序 |
| Java / Kotlin | Gradle 或 Maven 文件 | `jpackage` 应用目录；无法生成时保留 JAR 和启动脚本 |
| C / C++ | `CMakeLists.txt` | Release 原生程序 |
| Swift | `Package.swift` | macOS Release 程序 |
| Flutter / Dart | Flutter `pubspec.yaml` | Windows、Linux、macOS、APK、AAB |
| Android Java/Kotlin | Android Gradle 工程 | APK 和 AAB |
| React Native | `react-native` 和 `android/` | APK 和 AAB |
| Capacitor | `capacitor.config.*` | APK 和 AAB |
| Vue / React / Angular / Svelte | Node 构建脚本 | 平台无关的静态 Web 包；与后端混合时一起归档 |
| 其他语言 | `packaging.json` 自定义命令 | 任何命令产生的文件 |

默认桌面平台：

- Windows x64：`windows-latest`
- Linux x64：`ubuntu-latest`
- macOS arm64：`macos-latest`
- Android：Ubuntu Android SDK，生成 APK/AAB

可在 `packaging.json` 中增加 `macos-x64` 或 `linux-arm64`。iOS 也可以使用 macOS 自定义命令构建，但签名 IPA 必须自行配置 Apple 证书、Provisioning Profile 和相应 GitHub Secrets。

## 2. 最推荐的上传方法

### 第一步：从模板创建新仓库

1. 打开 `universal-auto-packager` 仓库。
2. 点击 **Use this template**。
3. 点击 **Create a new repository**。
4. 输入新程序的仓库名并创建。

这样新仓库会自动带有打包工作流。每个程序使用独立仓库，不会和以前的代码混在一起。

### 第二步：上传代码

网页方法：

1. 打开新程序仓库。
2. 点击 **Add file → Upload files**。
3. 上传程序源码。
4. 不要删除 `.github` 目录。
5. 点击 **Commit changes**。

Windows PowerShell 方法：

```powershell
git clone https://github.com/1346076596/你的新仓库.git
Set-Location 你的新仓库
Copy-Item -Path "D:\你的程序目录\*" -Destination . -Recurse -Force
git add -A
git commit -m "Upload program for automatic packaging"
git push origin main
```

Linux 或 macOS 方法：

```bash
git clone https://github.com/1346076596/你的新仓库.git
cd 你的新仓库
cp -R /你的程序目录/. .
git add -A
git commit -m "Upload program for automatic packaging"
git push origin main
```

不要上传 `.env`、密码、Token、签名证书或私钥。需要的密钥应保存在仓库的 **Settings → Secrets and variables → Actions** 中。

## 3. 自动打包发生什么

上传并推送到 `main` 后，工作流会：

1. 检测语言、框架和项目入口。
2. 根据需要安装 Python、Node.js、Java、Go、Rust、.NET 或 Flutter。
3. 在对应的 GitHub Runner 上构建。
4. 把每个平台的文件压缩成 ZIP 或 TAR.GZ。
5. 保存为 Actions Artifact，保留 30 天。
6. 自动创建 `auto-build-运行编号` 的预发布 Release。

下载位置：

- 仓库首页右侧 **Releases**。
- 或 **Actions → Universal Auto Package → 对应运行 → Artifacts**。

## 4. 自动识别入口规则

Python 会依次寻找：

```text
main.py
app.py
cli.py
__main__.py
```

Node.js 会读取 `package.json` 的 `bin`、`main`，然后寻找：

```text
dist/index.js
build/index.js
main.js
index.js
src/index.js
```

如果自动选择的入口不正确，请添加 `packaging.json`。

## 5. 单语言配置示例

在程序根目录创建 `packaging.json`：

```json
{
  "name": "MyProgram",
  "languages": ["python"],
  "targets": ["windows-x64", "linux-x64", "macos-arm64"],
  "versions": {
    "python": "3.12"
  },
  "components": {
    "python": {
      "entry": "src/main.py",
      "windowed": false,
      "addData": [
        {
          "source": "assets",
          "destination": "assets"
        }
      ]
    }
  }
}
```

支持的 `targets`：

```text
windows-x64
linux-x64
linux-arm64
macos-arm64
macos-x64
android
```

## 6. 混合代码项目

Python 后端 + Vue/React 前端、Rust + Node/Tauri、Java/Kotlin + Android 等项目，建议明确写出构建顺序。

下面是 Python + Vue + Capacitor 的完整例子：

```json
{
  "name": "MixedApplication",
  "languages": ["python", "node", "android"],
  "targets": ["windows-x64", "linux-x64", "macos-arm64", "android"],
  "versions": {
    "python": "3.12",
    "node": "22",
    "java": "21"
  },
  "commands": {
    "common": [
      {
        "cwd": "frontend",
        "run": "npm ci"
      },
      {
        "cwd": "frontend",
        "run": "npm run build"
      }
    ],
    "windows-x64": [
      {
        "run": "python -m pip install -r requirements.txt pyinstaller"
      },
      {
        "run": "python -m PyInstaller --noconfirm --onefile --name MixedApplication --add-data \"frontend/dist;frontend/dist\" launcher.py"
      }
    ],
    "linux-x64": [
      {
        "run": "python -m pip install -r requirements.txt pyinstaller"
      },
      {
        "run": "python -m PyInstaller --noconfirm --onefile --name MixedApplication --add-data \"frontend/dist:frontend/dist\" launcher.py"
      }
    ],
    "macos-arm64": [
      {
        "run": "python -m pip install -r requirements.txt pyinstaller"
      },
      {
        "run": "python -m PyInstaller --noconfirm --onefile --name MixedApplication --add-data \"frontend/dist:frontend/dist\" launcher.py"
      }
    ],
    "android": [
      {
        "cwd": "frontend",
        "run": "npx cap sync android"
      },
      {
        "cwd": "frontend/android",
        "run": "chmod +x gradlew && ./gradlew assembleDebug bundleDebug --no-daemon"
      }
    ]
  },
  "artifacts": {
    "windows-x64": ["dist/MixedApplication.exe"],
    "linux-x64": ["dist/MixedApplication"],
    "macos-arm64": ["dist/MixedApplication"],
    "android": [
      "frontend/android/app/build/outputs/apk/**/*.apk",
      "frontend/android/app/build/outputs/bundle/**/*.aab"
    ]
  }
}
```

只要存在 `commands`，通用打包器就会按 `common`、当前目标平台的顺序执行，并收集 `artifacts`。因此任何没有内置识别规则的语言也能使用。

每个命令还可以单独指定环境变量：

```json
{
  "run": "你的构建命令",
  "cwd": "子目录",
  "env": {
    "BUILD_MODE": "release"
  }
}
```

工作流自动提供：

```text
PACKAGING_TARGET   当前目标，例如 windows-x64
PACKAGING_OUTPUT   当前收集目录
PACKAGING_ROOT     源码根目录
```

## 7. 在已有仓库中调用本打包器

不使用模板时，只需在已有仓库创建 `.github/workflows/package.yml`：

```yaml
name: Package

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  package:
    uses: 1346076596/universal-auto-packager/.github/workflows/auto-package.yml@main
    permissions:
      contents: write
```

提交命令：

```bash
git add .github/workflows/package.yml packaging.json
git commit -m "Enable universal automatic packaging"
git push origin main
```

被调用的工作流会检出当前程序仓库，而不是把 `universal-auto-packager` 自己当成待打包程序。

## 8. Android、Windows 和 macOS 签名

默认目标是“可以测试和直接运行”：

- Android 在没有签名配置时会回退生成 Debug APK/AAB；Debug APK 可以直接侧载安装。
- Windows 未签名 EXE 可能触发 SmartScreen 提示。
- macOS 未公证程序可能触发 Gatekeeper 提示。
- iOS 不提供无证书的可安装 IPA。

正式发布需要把证书放入 GitHub Secrets，并通过 `packaging.json` 自定义签名命令。不要把证书密码写入代码或 JSON。

## 9. 常见失败处理

### 未识别到程序

添加 `packaging.json`，填写 `languages`、`targets`、`commands` 和 `artifacts`。

### 找不到 Python 入口

设置：

```json
{
  "components": {
    "python": {
      "entry": "实际入口.py"
    }
  }
}
```

### 打包成功但没有产物

检查 `artifacts` 是否指向真实文件。路径相对于仓库根目录，并支持 `**` 通配符。

### 依赖系统软件

使用 `commands` 安装或构建依赖。复杂 Linux 项目也可以在程序仓库中提供 Dockerfile，并把 Docker 构建命令写进配置。

### 原生程序不能跨系统直接复用

Windows、Linux、macOS 使用不同的二进制格式，所以工作流会分别在三个系统上构建。不能把 Windows EXE 当成 Linux 或 macOS 程序使用。

## 10. 更新模板

使用“已有仓库调用方式”的项目会自动使用中央仓库 `main` 分支上的最新打包器。需要完全固定版本时，把 `@main` 改成某个提交 SHA 或版本标签。
