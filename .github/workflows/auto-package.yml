name: Universal Auto Package

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  package:
    uses: 1346076596/universal-auto-packager/.github/workflows/auto-package.yml@main
    secrets: inherit
